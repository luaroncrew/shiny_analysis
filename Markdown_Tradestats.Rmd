---
title: "Markdown Tradestats"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Tradestats  

## Présentation du jeu de données  
Faire un summary, lister le nom des colonnes, le nombre de lignes, à quoi correspond les colonnes
## Le projet  
Expliquer le projet tradestats
la problematique
qu'est ce que la blockchain
## Exploration de données (stat univariées et bivariées)  
tableau avec les moyennes, mediane, min, max, pour le volume
des tableaux pour le nombre de lignes dans chaque catégorie
## Tableau  
## Visualisation (graphiques et interprétation) avec des commentaires  
graph de l'application et les analyses et explications
conclusion avec la réponse à la problématique
## Les options de bloc de code suivant sont utilisées au moins une fois (echo, eval, include, warning)  
